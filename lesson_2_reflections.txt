What happens when you initialize a repository? Why do you need to do it?
	
	A new folder .git is create. To keep track of the commits

How is the staging area different from the working directory and the repository? 
What value do you think it offers?

	The staging area is a step between both. Provides security organizing commits

How can you use the staging area to make sure you have one commit per logical change?

	Using git add filename any times as files reflect the logical change. Once all the 
	file that match a single logical change will be in staging area, git commit should
	be called

What are some situations when branches would be helpful in keeping your history organized? 
How would branches help?

	When you need to diverge your code, like implementing an experimental feature. 
	Branches help creating a single space where the experimental feature is developed, 
	while the master branch keep track of the rest of the tasks developed